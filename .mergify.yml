merge_queue:
  mode: serial
queue_rules:
  - name: VM2 native stack parent-edit freshness
    queue_conditions: []
    merge_conditions:
      - check-success = vm2-stack-integrity
    batch_size: 1
    merge_method: merge
